server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"

  profiles:
    active:
      - "prod"


build:
  version: "3.0"

accounts:
  message: "Welcome to my world default API's"
  contactDetails:
    name: "John Doe - Developer"
    email: "gopi.thochi@gmail.com"
  onCallSupport:
    -(040)26728
    -(91)9945544440
